#include <stdio.h>

int main(void)
{
	int i = 10;
	int j = 20;
	//i에 10, j에 20 저장
	int sum = i + j;
	//sum에 i와 j의 합 저장
	printf("%d + %d = %d\n", i, j, sum);
	/*i(10) + j(20) = sum(30)
	10 + 20 = 30 출력*/
	return 0;
}
